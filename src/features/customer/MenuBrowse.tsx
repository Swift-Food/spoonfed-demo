import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function MenuBrowse() {
  return <PlaceholderPage title="Menus for your date" note="Phase 1 — menusForDate() results as cards, with empty state." />;
}

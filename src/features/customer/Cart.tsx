import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function Cart() {
  return <PlaceholderPage title="Your cart" note="Phase 1 — line list, qty edit/remove, running subtotal, minimum checks." />;
}

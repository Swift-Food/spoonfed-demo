import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function MyOrders() {
  return <PlaceholderPage title="My orders" note="Phase 1 — order list with status chips and event dates." />;
}

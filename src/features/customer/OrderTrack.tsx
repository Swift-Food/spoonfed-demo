import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function OrderTrack() {
  return <PlaceholderPage title="Order tracking" note="Phase 1/2 — status timeline, order summary, edit/cancel actions." />;
}

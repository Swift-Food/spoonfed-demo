import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function DatePicker() {
  return <PlaceholderPage title="Choose your event date" note="Phase 1 — date picker that drives date-aware menu browsing." />;
}

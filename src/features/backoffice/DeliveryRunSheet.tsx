import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function DeliveryRunSheet() {
  return <PlaceholderPage title="Delivery run sheet" note="Phase 2 — stops sorted by delivery time, dispatch and delivered actions." />;
}

import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function Calendar() {
  return <PlaceholderPage title="Calendar" note="Phase 2 — week/day grid keyed by event date, colour-coded by status." />;
}

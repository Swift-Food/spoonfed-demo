import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function CreateOrderOnBehalf() {
  return <PlaceholderPage title="Create order on behalf" note="Phase 3 — pick an account, then build an order as back office." />;
}

import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function InvoiceList() {
  return <PlaceholderPage title="Invoices" note="Phase 2 — invoices with status, generate from delivered orders." />;
}

import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function MenuManager() {
  return <PlaceholderPage title="Menus" note="Phase 3 — menu list with CRUD and availability toggles." />;
}

import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function OrderDetailAdmin() {
  return <PlaceholderPage title="Order detail" note="Phase 2 — full order fields, status actions, history, driver assignment." />;
}

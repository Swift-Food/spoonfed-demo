import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function AccountDetail() {
  return <PlaceholderPage title="Account detail" note="Phase 3 — account config, contacts and order history." />;
}
